{
  "name": "parabank-ai-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "playwright test",
    "test:ui": "playwright test --project=ui",
    "test:api": "playwright test --project=api",
    "generate:bdd": "bddgen",
    "generate:tests": "tsx scripts/generate-tests.ts",
    "check:coverage": "tsx scripts/coverage-check.ts",
    "dashboard": "tsx utils/dashboard.ts",
    "send:report": "tsx utils/send-report-email.ts"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "nodemailer": "^6.10.0",
    "playwright-bdd": "^8.4.1",
    "zod": "^3.25.20"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0",
    "@types/node": "^22.15.16",
    "@types/nodemailer": "^8.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.8.3"
  }
}
