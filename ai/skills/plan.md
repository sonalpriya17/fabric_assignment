# Scenario Planning Rules

- Produce deterministic, testable, short steps
- Include setup, action, and verification phases
- Avoid speculative actions
- Prefer explicit business language

For plan requests, return one step per line.
For data requests, return valid minified JSON only.
